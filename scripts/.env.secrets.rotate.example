# Copie para "scripts/.env.secrets.rotate" (sem ".example") e
# preencha com valores reais -- NUNCA cole estes valores no chat.
# Está no .gitignore (nunca vai para o Git).
#
# SUPABASE_SERVICE_ROLE_KEY: Supabase Dashboard -> Project Settings -> API
# TEST_STAFF_PASSWORD aqui é a password NOVA que vai substituir "teste123"
# nas 12 contas -- escolha-a você, nunca vou sugerir nem gerar uma.
#
# Depois de preenchido, avise para eu correr (sem nunca imprimir o
# conteúdo do ficheiro):
#   set -a && source scripts/.env.secrets.rotate && set +a && node scripts/rotate-staff-passwords.mjs

SUPABASE_URL=https://qdfpqispcntitvczybfl.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
TEST_STAFF_PASSWORD=
